# Security Policy

Report vulnerabilities privately to the Mewra maintainers before opening a public issue.

Dependency Guard executes only scanner binaries selected by its fixed command builders. Docker mode uses pinned image digests and a read-only workspace mount. Do not report scanner findings from a customer repository publicly.
